os: linux

language: node_js

node_js:
  - lts/*

services:
  - docker

before_install:
  - docker build -t uanoob/web-app -f Dockerfile.dev .

script:
  - docker run -e CI=true uanoob/web-app npm run test
